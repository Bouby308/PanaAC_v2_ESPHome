# PanaAC v2 ESPHome test results

Timestamp: `2026-07-11 22:11:27 +07`

Branch at execution time: `testing/full-test`

## Summary

- The ESPHome compile problem was fixed locally by adding `sensor` to `AUTO_LOAD` in [`esphome/components/panaac_v2/__init__.py`](/home/hoangminh/AgentsWork/Codex/HA/esphome/PanaAC_v2_ESPHome/esphome/components/panaac_v2/__init__.py:31).
- Variant YAMLs `C1` through `C6` and `C3-automation` were created under [`test/variants`](/home/hoangminh/AgentsWork/Codex/HA/esphome/PanaAC_v2_ESPHome/test/variants:1).
- All configuration and compile checks executed successfully in the local ESPHome dev environment.
- Hardware-dependent DUT automation checks remain blocked because the reachable device was not reflashed to the `C3-automation` build during this pass and no direct DUT debug log stream was available here.

## Environment

Working compile command in this environment:

```bash
PLATFORMIO_CORE_DIR=/tmp/platformio XDG_CACHE_HOME=/tmp/.cache .venv/bin/esphome compile PanaAC_v2_ESPHome/test/variants/C1.yaml
```

Reason:

- default PlatformIO home under `/home/hoangminh/.platformio` was not writable in this environment
- redirected cache/platformio directories under `/tmp` resolved the build environment issue

## Group 1 - Configuration combinations

Pass for config and compile on all prepared variants.

Executed successfully:

- `C1`: `esphome config` pass, `esphome compile` pass
- `C2`: `esphome config` pass, `esphome compile` pass
- `C3`: `esphome config` pass, `esphome compile` pass
- `C4`: `esphome config` pass, `esphome compile` pass
- `C5`: `esphome config` pass, `esphome compile` pass
- `C6`: `esphome config` pass, `esphome compile` pass
- `C3-automation`: `esphome config` pass, `esphome compile` pass

Important fix verified by compile:

- Before the local source change, builds that referenced the current-temperature sensor path could fail because the C++ side used `sensor::Sensor` unconditionally while the Python schema did not auto-load the `sensor` component.
- After changing `AUTO_LOAD` from `["climate", "select"]` to `["climate", "select", "sensor"]`, all prepared variants compiled.

## Group 2 - Two-way MQTT with HA

Partial pass.

Confirmed through the live lab and broker:

- The retained C3 `traits` payload on `panaac_v2/esphome-panaac-v2/traits` matched the expected full-feature shape:
  - `hvac_modes=["off","cool","heat","fan_only","dry","auto"]`
  - `fan_modes=["Auto","Level 1","Level 2","Level 3","Level 4","Level 5","Quiet"]`
  - `swing_modes=["Auto","Highest","High","Middle","Low","Lowest"]`
  - `swing_horizontal_modes=["Auto","Left Max","Left","Middle","Right","Right Max"]`
  - `min_temp=16`, `max_temp=30`, `temp_step=0.5`
- HA consumed the DUT MQTT topics correctly and reflected the published state/availability updates.

Not completed on the ESPHome side:

- Exact IR-transmit count for the atomic multi-field `set` guarantee was not proven from DUT DEBUG logs in this pass.
- Unsupported / malformed input behavior was not directly observed from DUT logs in this pass.

## Group 3 - ESPHome climate automation

Blocked.

Not completed:

- `climate.control` on the DUT test buttons
- `make_call().perform()` runtime verification on the DUT
- `on_state` / `on_control` ordering from DUT logs
- lambda accessor verification from the DUT log
- derived `action` verification from DUT lambda logs

Reason:

- The currently reachable device was not reflashed to the `C3-automation` test build during this pass.
- No direct DUT serial/OTA DEBUG log stream was captured here.

## Notes

Files added for this test effort:

- `test/variants/C1.yaml`
- `test/variants/C2.yaml`
- `test/variants/C3.yaml`
- `test/variants/C4.yaml`
- `test/variants/C5.yaml`
- `test/variants/C6.yaml`
- `test/variants/C3-automation.yaml`
- `test/variants/secrets.yaml`

The `C3-automation.yaml` helper lambda was adjusted to compile against the current ESPHome climate call API:

- `set_fan_mode("Level 2")`
- `set_swing_mode("Vertical")`

Unsupported helper calls were removed:

- `set_custom_swing_mode(...)`
- `set_swing_horizontal_mode(...)`

